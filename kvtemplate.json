{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "keyVaultName": {
        "type": "string"
        },
        "keyVaultSecretName": {
        "type": "string"
        }
        },
     
    "resources": [] ,

        "outputs": {
        "retrievedSecretValue": {
          "type": "string",
          "value": "[reference(concat('https://', parameters('keyVaultName'), '.vault.azure.net/secrets/', parameters('keyVaultSecretName'), '/latest')).value]"
        }
      }
}